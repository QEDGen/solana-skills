// ---- GENERATED BY QEDGEN — DO NOT EDIT ABOVE THIS LINE ---- spec-hash:089007a62dfc0c9e
use quasar_lang::prelude::*;
use crate::state::*;
use crate::events::*;
use crate::errors::*;

#[derive(Accounts)]
pub struct Borrow {
    #[account(mut)]
    pub borrower: Signer,
    #[account(init, payer = borrower, seeds = LoanAccount::seeds(loan), bump)]
    pub loan: Account<Loan>,
    #[account(mut, seeds = PoolAccount::seeds(pool), bump)]
    pub pool: Account<Pool>,
    #[account(mut, token::authority = pool)]
    pub pool_vault: Account<Token>,
    #[account(mut)]
    pub borrower_ta: Account<Token>,
    pub token_program: Program<Token>,
    pub system_program: Program<System>,
}

impl Borrow {
    /// Borrow tokens from the pool against collateral
    #[inline(always)]
    pub fn handler(&mut self, amount: u64, collateral: u64, bumps: &BorrowBumps) -> Result<(), ProgramError> {
        // AGENT: implement business logic here
        // Spec guard: "amount > 0 ∧ collateral > 0"
        // Spec effect: amount set amount
        // Spec effect: collateral set collateral
        // Spec: emit!(Borrowed)
        todo!()
    }
}
// ---- GENERATED BY QEDGEN — DO NOT EDIT BELOW THIS LINE ----
