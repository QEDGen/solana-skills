// ---- GENERATED BY QEDGEN — DO NOT EDIT ---- spec-hash:d7a363b8124408d3
use quasar_lang::prelude::*;

#[account(discriminator = 1, set_inner)]
#[seeds(b"pool", authority: Address)]
pub struct PoolAccount {
    pub authority: Address,
    pub total_deposits: u64,
    pub total_borrows: u64,
    pub interest_rate: u64,
    pub bump: u8,
}

/// Pool lifecycle states.
#[derive(Clone, Copy, PartialEq, Eq)]
#[repr(u8)]
pub enum PoolStatus {
    Uninitialized = 0,
    Active = 1,
    Paused = 2,
}

#[account(discriminator = 2, set_inner)]
#[seeds(b"loan", pool: Address, borrower: Address)]
pub struct LoanAccount {
    pub borrower: Address,
    pub pool: Address,
    pub amount: u64,
    pub collateral: u64,
    pub bump: u8,
}

/// Loan lifecycle states.
#[derive(Clone, Copy, PartialEq, Eq)]
#[repr(u8)]
pub enum LoanStatus {
    Empty = 0,
    Active = 1,
    Liquidated = 2,
}

// ---- END GENERATED ----
